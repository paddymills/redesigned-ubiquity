
#![warn(missing_docs)]

//! High Steel system integration components
//! 
//! features (none are default)
//!   - `terminal` enables logging to [`std::io::stdout`]

pub mod config;
pub mod db;
pub mod logging;
